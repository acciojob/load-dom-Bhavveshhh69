//your JS code here. If required.
window.addEventListener('DOMContentLoaded', () => {
  // Write the required string to the page when DOM is loaded
  document.body.textContent = "DOM load success";
});
